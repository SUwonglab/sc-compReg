"mfbs" <- function(elem.name,
                   symbol,
                   motif.name,
                   motif.weight,
                   match2,
                   motif.file,
                   ...) {
    UseMethod("mfbs")
}
