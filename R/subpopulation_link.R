"subpopulation_link" <- function(E.mean.healthy,
                               E.mean.cll,
                               O.mean.healthy,
                               O.mean.cll,
                            ...) {
    UseMethod("subpopulation_link")
}
