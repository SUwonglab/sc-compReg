subpopulation.link <- function(E.mean.healthy,
                               E.mean.cll,
                               O.mean.healthy,
                               O.mean.cll,
                            ...) {
    UseMethod("subpopulation.link")
}
